# `/inspect`

When to use:

- Immediately after a failed run to review what was captured.
- Before recording failure memory.

Examples:

```text
/inspect
/inspect --errors
/inspect --files --git --json
```
