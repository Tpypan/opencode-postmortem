# `/postmortem-config`

When to use:

- You want to check or set postmortem storage behavior.

Examples:

```text
/postmortem-config --action show --json
/postmortem-config --action set --storage repo --json
/postmortem-config --action set --storeRaw --json
```
