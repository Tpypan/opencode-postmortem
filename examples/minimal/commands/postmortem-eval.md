# `/postmortem-eval`

When to use:

- You want local repeat-failure metrics to evaluate whether rules are reducing recurrence.

Examples:

```text
/postmortem-eval
/postmortem-eval --window 20 --json
```
