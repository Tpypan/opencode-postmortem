# `/retry`

When to use:

- You want a guardrailed retry prompt derived from active rules and recent context.

Examples:

```text
/retry
/retry --explain
/retry --skip <rule-id> --skip <rule-id> --explain
/retry --yes --json
```
